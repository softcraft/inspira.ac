jQuery(document).ready(function() {
    // Home Accordion
    jQuery('.facebook-list').raccordion({
       speed        : 1000,
       sliderWidth  : 1000,
       sliderHeight : 300,
       autoCollapse : true
   });
});
